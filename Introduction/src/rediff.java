import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class rediff {

	public static void main(String[] args) {
		
		
		 System.setProperty("webdriver.chrome.driver",
				  "C:\\chromedriver.exe");
				  
				  WebDriver driver = new ChromeDriver();
			//	  driver.get("http://rediff.com");
			//	  driver.findElement(By.linkText("Sign in")).click();

			//	  driver.findElement(By.xpath("//a[@title='Already a user? Sign in']")).click();
				  
			//	  driver.findElement(By.cssSelector("input[name='login']")).sendKeys("linta shijo");
			//	  driver.findElement(By.xpath("//input[@id='password']")).sendKeys("mynew password");
			//	  driver.findElement(By.xpath("//a[@class='f12']")).click();
				  
				  
				  
		

	}

}
